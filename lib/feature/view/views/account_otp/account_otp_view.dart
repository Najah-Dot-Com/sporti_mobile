import 'package:flutter/material.dart';
class AccountOtpView extends StatelessWidget {
  const AccountOtpView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}