import 'package:flutter/material.dart';
class CategoriesMyWorkView extends StatelessWidget {
  const CategoriesMyWorkView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}